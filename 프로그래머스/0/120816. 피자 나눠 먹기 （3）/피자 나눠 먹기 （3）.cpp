#include <string>
#include <vector>

using namespace std;

int solution(int slice, int n) {
    int answer = 0;
    for (auto i=1; i<100; i++){
        if(slice*i>=n) return i;
        
    }
    // return answer;
}